Ejecuta comandos kubectl para gestionar Kubernetes.
Sin args: `kubectl get all`. "pods": `kubectl get pods`. "logs POD": `kubectl logs POD`.
"apply FILE": `kubectl apply -f FILE`. "contexts": `kubectl config get-contexts`.
$ARGUMENTS
